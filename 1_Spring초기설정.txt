마켓 > sts > spring 맨 위에꺼

프로젝트 생성
Spring Legacy Project > Spring MVC Project 
> topLevelPackage(최상의 스프링 패키지인데 수업땐 kr.or.ddit로 통일)

프로젝트 생성시
프로젝트 Properties
>> Deployment Assembly > Add > Java Build Path > Next > 비어있는거 확인
>> Java Build Path > JRE > 1.8 > Add Library > Server Runtime > next > Apache > finish
>> Project Facets > Dynamic Web Module - 3.1 > Java - 1.8

pom.xml > java-version > 1.8
바로 아래 springframeword-version > 5.2.5

프로젝트 우클릭 > Run As > Maven Build > Goals - compile > run

톰캣서버
>> 포트 80
>> Timeouts > start(위에꺼) > 120

web.xml(main)
>> web-app version="3.1" > xsi:schemaLocation=".../web-app_3_1.xsd"
아래 붙여넣기
<!-- 한글 처리 -->
	<filter>
		<filter-name>encodingFilter</filter-name>
		<filter-class>
			org.springframework.web.filter.CharacterEncodingFilter
		</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>UTF-8</param-value>
		</init-param>
		<init-param>
			<param-name>forceEncoding</param-name>
			<param-value>true</param-value>
		</init-param>
	</filter>
	<filter-mapping>
		<filter-name>encodingFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>

log4j(main)
>> 맨 아래 root - priority - value - debug로 수정 (디버깅용)
